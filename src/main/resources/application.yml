spring:
  jpa:
    hibernate:
      ddl-auto: update #none
    properties:
      hibernate:
        default_batch_fetch_size: 100

  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
